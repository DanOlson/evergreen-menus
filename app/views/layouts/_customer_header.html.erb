<ul class="nav navbar-nav">
  <%= tab name: 'Establishments', path: account_path(account) %>
  <% if can?(:view, User) %>
    <%= tab name: 'Staff', path: account_users_path(account) %>
  <% end %>
</ul>
<ul class="nav navbar-nav navbar-right">
  <li>
    <%= link_to edit_user_registration_path, data: { test: 'profile-link' } do %>
      <span class="glyphicon glyphicon-user" aria-hidden="true"></span> <%= current_user.first_name %>
    <% end %>
  </li>
  <li><%= link_to 'Logout', destroy_user_session_path %></li>
</ul>
